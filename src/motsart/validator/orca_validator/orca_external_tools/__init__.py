"""External-tool wrappers for ORCA's ``otool_external`` (``! ExtOpt``) interface.

``mlip_external.py`` exposes an OMol25 MLIP (e.g. eSEN-sm-conserving) as a PES
engine that ORCA's own optimizers can drive. See that module's docstring for
the I/O contract.
"""
